# CLAUDE.md

Bitmapper is a Flutter app (Android and iOS) that applies a retro pixel-art
filter, with a Windows 98 UI. Flutter is pinned by asdf in `.tool-versions`
(3.41.4).

## Commands

- `flutter test`: app tests. `flutter analyze` must stay clean.
- `cd packages/bitmapper_core && dart test`: engine tests (pure Dart).
- `cd packages/win98_ui && flutter test`: kit tests. Goldens are in
  `test/goldens/`; regenerate them with
  `flutter test --tags golden --update-goldens`.
- `flutter gen-l10n`: run after editing `lib/l10n/app_{en,pt}.arb`. The
  output in `lib/l10n/generated` is committed.

## Architecture

- **`packages/bitmapper_core`** is a port of `../bitmapper` (Python), one
  file per Python module.
  - It must not import Flutter.
  - Numeric contracts from `../bitmapper/docs/FLUTTER_MIGRATION.md` apply:
    - `splitSizes` follows `np.array_split` semantics.
    - Byte conversion truncates (`clampToByte`); never round.
    - Diffusion weights are pre-scaled as `(w*s)/d`.
    - `subsample` rounds half to even.
  - Several tests assert exact bytes produced by the Python code. If one
    fails, the port has drifted.
  - `palettes_data.dart` is generated by `tool/gen_palettes.py`. Do not edit
    it by hand.
- **`packages/win98_ui`** depends only on `flutter/widgets.dart` (no
  Material) so it can be reused elsewhere. Keep it free of app code. Every
  control is built on `Bevel`/`BevelPainter`.
- **App (`lib/`):**
  - State uses Provider with `ChangeNotifier`.
  - `EditorModel` owns the `BitmapFilterConfig`.
  - `MediaModel` holds a still (the full source plus a preview copy of at
    most 1024 px) or an animation.
  - `PresetsModel` holds built-in and user presets.
  - `FilterController` debounces preview renders, keeps one run in flight
    with the latest request winning, and drops results for a replaced
    source.
  - `HomeScreen` listens to the editor and media models and calls
    `FilterController.request`. For animations it also passes an
    `AnimationContext` (the frames plus the current frame index).
  - `MediaModel` holds either a still or an animation. For an animation it
    keeps the decoded frames plus the original GIF bytes.
  - `FilterController` caches the shared animation palette, keyed by
    `paletteKeyFor`, so scrubbing and changes to dither, scanlines or gaps
    reuse it.
  - GIF export (`animation_exporter.dart`) sends the GIF bytes to a worker
    isolate, which decodes, filters and encodes them. Progress arrives over
    a `ReceivePort`, and cancel kills the isolate.
  - `exportGif` is the pure core of the export; tests call it directly.
  - Engine: `sequence.dart` provides `paletteSourceFrames`,
    `sequencePalette` and `frameSeed`. `applyBitmapFilter` takes an optional
    `palette` and `seed`.
  - Export uses `configFor(preview size)`, so the grid matches the preview,
    rendered at full source size.
- **Side effects** (picker, saver, preset storage, filter runner, PNG
  encoder, clock) are injected through `AppServices`. Tests use the fakes in
  `test/helpers.dart`.
  - `syncRunner` replaces `Isolate.run`, which never completes under the
    widget tester's fake async.
- **Preview display:** `RgbImageView` wraps the pixels in an uncompressed
  BMP for `Image.memory`, with `FilterQuality.none`.

## Testing notes

- For widget tests, call `usePhoneScreen(tester)` (a 360×780 viewport) and
  build the app with `TestApp().build()`.
- The Flutter test font renders every glyph 1em wide, which makes text much
  wider than the real pixel font. Layouts must tolerate that: menu bars and
  tabs wrap.
- Popups (menus, drop-downs) render in the Overlay. To see them in golden
  screenshots, capture from `WidgetsApp`.
